# 🖼️ Image Processing Tool

這是一個基於 Python 與 PyQt5 開發的數位影像處理作業專案 (Digital Image Processing Project)。
專案包含圖形化介面 (GUI)，能夠載入圖片並執行多種空間域與頻率域的濾波處理，同時提供測試影像與實驗報告。

## 📂 檔案結構 (File Structure)

本專案的目錄結構如下：

```text
image_processing/
├── code/                  # 程式碼與測試影像目錄
│   ├── .vscode/           # VSCode 設定檔
│   ├── figure1.jpg        # 測試影像 1
│   ├── figure2.jpg        # 測試影像 2
│   └── my.py              # 主程式執行檔 (Main Entry Point)
```

## ✨ 功能特色 (Features)

程式介面包含左側控制面板與右側 2x2 的影像顯示區，主要功能如下：

  * **📂 載入圖片 (Load Image)**：支援讀取 `.jpg`, `.png`, `.bmp` 等常見格式 (包含目錄下的 `figure1.jpg` 與 `figure2.jpg`)。
  * **💧 平滑化濾波 (Smooth Filter)**：
      * **Average Filter** (空間域)：5x5 均值模糊。
      * **Median Filter** (空間域)：中值濾波，去除雜訊。
      * **Ideal Low-pass Filter** (頻率域)：傅立葉轉換低通濾波。
  * **🔪 銳化濾波 (Sharp Filter)**：
      * **Sobel Operator** (空間域)：邊緣偵測。
      * **Ideal High-pass Filter** (頻率域)：傅立葉轉換高通濾波。
  * **🌫️ 高斯模糊 (Gaussian)**：空間域高斯模糊。
  * **📉 高斯低通濾波 (Lower-pass)**：頻率域高斯低通濾波 (Gaussian Low-pass)。

## 🛠️ 系統需求 (Requirements)

請確保環境已安裝 Python 3.x 以及下列函式庫：

  * opencv-python
  * numpy
  * PyQt5

## 🚀 安裝與執行 (Installation & Usage)

### 1\. 安裝依賴套件

```bash
pip install opencv-python numpy PyQt5
```

### 2\. 執行程式

請依照以下步驟進入 `code` 資料夾並執行主程式 `my.py`：

```bash
# 1. 進入專案目錄
cd image_processing

# 2. 進入程式碼目錄
cd code

# 3. 執行 Python 腳本
python my.py
```

### 3\. 操作測試

程式啟動後，點擊 **"Load Image"**，你可以直接選擇同目錄下的 `figure1.jpg` 或 `figure2.jpg` 進行測試。

-----
## Demo影片

https://github.com/user-attachments/assets/9ce032fe-e26b-407d-97ed-f8929886aa43


